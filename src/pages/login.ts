import { login } from "/Users/tibet/OneDrive/Belgeler/GitHub/smp-frontend/src/api/auth";

document.getElementById("form-section")?.addEventListener("submit",async (e) =>{
    e.preventDefault();

    // TODO: Continue after discussing with the group.
    //const email = (document.getElementById())
});
